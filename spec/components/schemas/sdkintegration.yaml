components:
  schemas:
    SDKIntegration:
      type: object
      description: holds metadata about a plugin/integration on the app agent that collected and sent the event.
      properties:
        name:
          type: string
          description: Integration name
          x-go-type-skip-optional-pointer: true
        version:
          type: string
          description: Integration version
          x-go-type-skip-optional-pointer: true
